import {Colors} from '@styles';
import styled from 'styled-components/native';

export const OnboardingStyle = styled.View`
  align-items: center;
  justify-content: center;
  flex: 1;
  flex-direction: column;
  background: ${Colors.background};
`;
